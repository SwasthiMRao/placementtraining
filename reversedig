#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, first, last, m, final;
    scanf("%d", &n);
    first= n/100;
    last= n % 10;
    m = (n/10)-(first*10);
    final= last*100 + m*10 + first;
    printf("%d", final);
    return 0;
}