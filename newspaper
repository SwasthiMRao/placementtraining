#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int a, b, c, sat;
    scanf("%d\n%d\n%d", &a, &b, &c);
    sat= (a*b)-(a*c)-100;
    printf("%d", sat);
    return 0;
}