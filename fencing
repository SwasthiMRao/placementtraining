#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int l, b, pr, ar;
    scanf("%d %d", &l, &b);
    pr= 2*(l+b);
    ar= l*b;
    printf("The required length is %d m\nThe required area of carpet is %d sqm", pr, ar);
    return 0;
}